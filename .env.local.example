# Copia este archivo a .env.local y pon tus claves reales.
# El archivo .env.local NUNCA debe subirse a git (ya está en .gitignore).

ANTHROPIC_API_KEY=sk-ant-...

# Opcionales, solo si quieres habilitar esos modelos:
OPENAI_API_KEY=
GOOGLE_API_KEY=
